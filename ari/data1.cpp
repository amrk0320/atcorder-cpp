#include <bits/stdc++.h>
using namespace std;

int main() {
  int n, l, p;
  int ans = 0;
  int now = 0;
  cin >> n >> l >> p;
  vector<int> stands(n);
  vector<int> gasolin(n);
  priority_queue<int> q;
  cout << -1 << endl;
}
