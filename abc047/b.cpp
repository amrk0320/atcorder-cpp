#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main() {
  int n, l, r, d, u, w, h;
  cin >> w >> h >> n;
  l = d = 0;
  r = w;
  r = h;
  vector<int> x(n);
  vector<int> y(n);
  vector<int> a(n);
  for (int i = 0; i < n; i++){
    cin >> x.at(i) >> y.at(i) >> a.at(i);
  }
}
