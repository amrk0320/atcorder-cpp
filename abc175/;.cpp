#include <bits/stdc++.h>
using namespace std;
 
int main() {
  int n, k;
  int n, k;
  cin >> n >> k;
  vector<int> P;
  vector<int> C;
  for (int i = 0; i < n; i++){
    cin >> P.at(i);
  }
  for (int i = 0; i < n; i++){
    cin >> P.at(i);
  }
  int q = k/n;
  if (q < 1) {

  }
}